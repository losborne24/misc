{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "ask-inline: build",
      "type": "shell",
      "command": "yarn build",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": "$esbuild",
      "group": "build"
    }
  ]
}
